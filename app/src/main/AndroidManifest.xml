<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mymoney">

    <application
        android:name=".framework.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMoney">
        <activity
            android:name=".presenter.SplashScreen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presenter.RegisterUser"
            android:exported="true">

        </activity>
        <activity
            android:name=".presenter.CreateStatement"
            android:exported="true" />
        <activity
            android:name=".presenter.EconomyView"
            android:exported="false" />
        <activity
            android:name=".presenter.CreateEconomy"
            android:exported="false" />
        <activity
            android:name=".presenter.BankList"
            android:exported="true"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>